#include <bits/stdc++.h>
using namespace std;
 
int main() {
	int t,l,b;
	cin >> t;
	while(t--)
	{
		cin >> b >> l;
		cout << sqrt( l*l - b*b ) << " " <<  sqrt( l*l + b*b ) << endl;
		
		
		
		
		
	}
	return 0;
} 
